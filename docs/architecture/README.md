# CHECK Architecture

Architecture documentation lives here.

The initial repository deliberately contains no MCP server, UI,
database, authentication system, persistence layer, or deployment
infrastructure.

Those components will only be introduced if the CHECK V1 design
demonstrates that they are necessary.
