# Artifact Fixtures

This directory will contain artifacts with known coverage, omissions,
contradictions, and unverifiable claims.

Fixtures will be added after the CHECK verification contract is locked.
