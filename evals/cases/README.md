# Evaluation Cases

The source cases live under `tests/fixtures/` and `tests/adversarial/`. This directory is reserved for any later ChatGPT-specific invocation wrappers that do not belong in the semantic ground-truth fixtures.
